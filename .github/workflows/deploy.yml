name: build-app-action
on: 
  push:
    branches:
      - main
jobs:
  build:
    name: CI part
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
  
  deploy:
    name: CD part
    runs-on: ubuntu-latest
    strategy:
      matrix:
        app-name: ['projectx']
    needs: build
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
      - name: Work ssh conections
        run: |
          ssh ec2-user@ec2-54-194-176-160.eu-west-1.compute.amazonaws.com
