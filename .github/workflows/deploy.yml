name: build-app-action
on: 
  push:
    branches:
      - main
jobs:
  build:
    name: CI part
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
  
  deploy:
    name: CD part
    runs-on: ubuntu-latest
    strategy:
      matrix:
        app-name: ['projectx']
    needs: build
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
      - name: Work ssh conections
        run: |
          pwd
          ssh sysadmin@212.111.202.6
