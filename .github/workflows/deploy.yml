name: build-app-action
on: 
  push:
    branches:
      - development
jobs:
  build:
    name: CI part
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
  
  deploy:
    name: CD part
    runs-on: ubuntu-latest
    steps:
      - name: ssh Conection
        run: |
          ssh sysadmin@212.111.202.6
          ls -la
