name: build-app-action
on: 
  push:
    branches:
      - test
jobs:
  build:
    name: CI part
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
  
  deploy:
    name: CD part
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
      - name: Copying appspec.yml
        run: |
          pwd
          ls -la
